<h1>Products</h1>
<section id="products">
  <div class="row">
    <div class="col-md-12">   
      <% if @products.any? %>
        <%= render @products %>
      <% else %>
        <div class="well">
          No products yet
        </div>
      <% end %>
    </div>
  </div>
</section>